{
  "name": "api.video-browser-to-rtmp",
  "description": "Libraries to broadcast the stream of a webcam from the browser to an RTMP server.",
  "version": "0.2.4",
  "author": "api.video <ecosystem@api.video> (https://api.video/)",
  "repository": {
    "type": "git",
    "url": "git@github.com:apivideo/api.video-browser-to-rtmp.git"
  },
  "license": "MIT",
  "keywords": [
    "video",
    "webcam",
    "rtmp",
    "libestream",
    "apivideo"
  ],
  "scripts": {
    "test": "npm run test --workspace=server  --workspace=client",
    "tslint": "npm run tslint --workspace=server  --workspace=client",
    "build": "npm run build --workspace=server  --workspace=client",
    "start": "npm run start --workspace=example"
  },
  "workspaces": [
    "client",
    "server",
    "example"
  ],
  "devDependencies": {
    "@types/chai": "^4.2.14",
    "@types/mocha": "^9.0.0",
    "chai": "^4.2.0",
    "mocha": "^9.1.3",
    "ts-node": "^9.1.1",
    "tslint": "^6.1.3",
    "typescript": "^4.5.4"
  },
  "dependencies": {
  }
}
